# Development Environment Variables for Vue App
# Used when running locally with npm run dev

# API Base URL - Empty means same origin (using Vite proxy)
VITE_API_BASE_URL=https://api.deanyang.it.com
